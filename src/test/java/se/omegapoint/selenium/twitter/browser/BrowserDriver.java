package se.omegapoint.selenium.twitter.browser;

import org.openqa.selenium.WebDriver;

/**
 * Interface for the WebDriver instance
 */
public interface BrowserDriver {
    WebDriver getDriverInstance();
}
